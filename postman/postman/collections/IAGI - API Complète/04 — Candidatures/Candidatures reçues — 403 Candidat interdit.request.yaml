$kind: http-request
url: "{{base_url}}/offres/{{offre_id}}/candidatures"
method: GET
headers:
  Accept: application/json
  Authorization: Bearer {{token_candidat}}
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 403 — candidat cannot view', function () {
          pm.response.to.have.status(403);
      });
    language: text/javascript
order: 7000
