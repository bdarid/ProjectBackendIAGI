$kind: http-request
url: "{{base_url}}/profil/me"
method: GET
headers:
  Accept: application/json
  Authorization: Bearer {{token_candidat}}
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 200 — profil returned', function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 1000
