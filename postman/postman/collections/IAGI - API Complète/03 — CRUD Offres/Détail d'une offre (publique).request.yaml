$kind: http-request
url: "{{base_url}}/offres/{{offre_id}}"
method: GET
headers:
  Accept: application/json
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 200 — offre detail', function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 4000
